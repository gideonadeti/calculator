export interface KeyProps {
  keyData: {
    id: string;
    value: string;
  };
}

export interface KeysProps {
  keysData: {
    id: string;
    value: string;
  }[];
}

export interface DisplayProps {
  input: string;
  output: string;
}
